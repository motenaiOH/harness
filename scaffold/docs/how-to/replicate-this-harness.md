# Replicate this harness

> **Seed page (How-to).** TODO: write the task-oriented steps to bootstrap a new
> project from this scaffold for YOUR domain. A how-to assumes competence and
> solves one concrete goal — keep it a numbered checklist, not a tutorial.

## Goal

Bootstrap a new `<App>` from the scaffold and replace the placeholders with your
domain.

## Steps

1. TODO: copy the scaffold and rename the placeholders (`<App>`, `<Feature>`, `<Entity>`).
2. TODO: set up env from `.env.example`.
3. TODO: run the Definition-of-Done gates and confirm they pass.
